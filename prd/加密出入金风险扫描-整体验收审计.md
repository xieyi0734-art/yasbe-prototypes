# 整体验收审计 — 加密出入金钱包风险扫描（Merkle Science）

> 审计日期：2026-09-10 ｜ v1.1 重审：2026-09-14 ｜ v1.2 复审：2026-09-18 ｜ **v1.3 复审（阈值二次修订）：2026-09-18**
> 审计范围：需求梳理 → 客户端原型 → 管理端原型 → PRD 的完整交付链路
> 审计依据：用户原始需求 + Merkle Science Compass API v4 文档 + **2026-09-14 管理端现网实测**（导航结构 / 真实字段 / 设计 token）

---

## 修订记录

- **v1.0（2026-09-10）**：按 09-10 版原型（管理端「入金/出金两条队列」）审计，结论「整体验收通过」。
- **v1.1（2026-09-14）**：管理端原型按**现网实测**重写为 `2026-09-14_加密出入金风险扫描_管理端.html`（单表 + 类型筛选 + 15 列 + 4 统计卡 + MS 风险信息弹窗 + 审批闸门 + 手动重新扫描），据此重审；PRD 升 **v1.2**、需求梳理同步更新。
  - v1.0 中以下条目**已失效**，本版按现状改写：①「入金/出金两条队列」→ 单表 + 类型筛选；②「Merkle 风险明细含置信度/直接间接来源」→ 弹窗 6 字段（逐条明细列为后续增强 D10）；③「资金状态块」→ 以交易状态与闸门承载；④ 关联原型指向 09-10 → 09-14。
- **v1.3（2026-09-18 二次修订）**：风险阈值由 **0–3 自动通过 / 4–5 转人工** 修订为 **0–2 自动通过 / 3–5 转人工**（Medium(3) 起转人工复核）；管理端 2 笔 Medium 存量单由「已自动放行」改置「处理中 · 待复核」；「筛查未完成」统计卡改为按**扫描状态**（未扫描）计数，避免与待复核行混淆；实测统计卡 **待人工复核 6 / 已自动通过 10 / 筛查未完成 5 / 退款处理中 1**。PRD 升 **v1.4**、需求梳理同步更新。
  - v1.3 附带**口径对齐**（同次审计发现，代码为真值）：操作列「复审」实际判定 = 交易状态「处理中」且类型为充提 → 共 **11 行**（6 行 3–5 级待复核 + 5 行扫描未完成：扫描中 2 / 失败 1 / 待扫描 2）；原 PRD §5 表述「3–5 级且扫描已完成才显示复审」窄于实现，已修正为与原型一致 —— 未完成行必须保留「复审」入口，否则失败 / 未扫描单无处置入口、会永久停在「处理中」无人认领。
- **v1.2（2026-09-18）**：原型新增「**拒绝理由闭环 + 客户端回显**」（管理端拒绝必填理由并落库、客户端详情回显）；同步原型已落地口径 —— 风险阈值改为 **0–3 自动通过 / 4–5 不自动通过转人工**、管理端列表 **16 列**（新增「退款状态」）、**入金拒绝后后台自动原路退款**（退款中 → 已退款 / 退款失败）。PRD 升 **v1.3**、需求梳理同步更新。
  - v1.1 中以下条目**已失效**：①「阈值 `risk_level ≥ 3` 拦截」→ 现行统一口径为「0–2 自动通过 / 3–5 转人工」（2026-09-18 二次修订，Medium(3) 起拦截）；②「单表 15 列」→ 16 列；③「统计卡 待扫描/扫描中/中高风险待处置/已完成」→ 现行 4 张统计卡：待人工复核 3–5 级 / 已自动通过 0–2 级 / 筛查未完成 / 退款处理中；④「MS 风险评分 x/10」→ 已删除（Merkle 无评分字段）。

---

## 一、需求覆盖度审计（对照用户原始需求）

| # | 用户原始需求 | 覆盖情况 | 落点（v1.1） |
|---|---|---|---|
| 1 | 出入金时扫描对方钱包 | ✅ 已覆盖 | F-001（入金发送方）/ F-002（出金接收方） |
| 2 | 风险钱包入金命中 → 不立即入账 → 待审 | ✅ 已覆盖 | F-003 / §7.2：交易状态「处理中」+ 挂账、闸门禁止审批 |
| 3 | 运营可拒绝，拒绝后退还 | ✅ 已覆盖 | F-006 拒绝（理由必填）→ 交易状态「已拒绝」+ 后台原路退款 |
| 4 | 出金检测对方钱包 → 风险 → 待审 | ✅ 已覆盖 | F-002 / F-003 / §7.3 |
| 5 | 出金运营拒绝 → 出金失败 | ✅ 已覆盖 | F-007：资金解冻退回可用余额，不可自动重试 |
| 6 | 针对 API 文档梳理需求 + 注意点 | ✅ 已覆盖 | `prd/Merkle-Science-加密出入金风险扫描-需求梳理与注意点.md`（09-14 更新） |
| 7 | 客户端原型 | ✅ 已覆盖 | `2026-09-11_加密出入金风险扫描_客户端.html` |
| 8 | 管理端原型 | ✅ 已覆盖 | `2026-09-14_加密出入金风险扫描_管理端.html` |
| 9 | PRD | ✅ 已覆盖 | `prd/PRD-加密出入金钱包风险扫描-Merkle-Science-v1.0.md`（文档版本 v1.4） |

**结论**：用户 5 条业务硬约束全部覆盖，4 项交付物全部产出，无遗漏。

---

## 二、业务规则合规审计（硬约束逐条核验）

| 硬约束 | 原型体现（09-14 管理端 / 09-11 客户端） | PRD | 判定 |
|---|---|---|---|
| 风险命中 = 不自动入账 | 交易状态「处理中」（挂账）+ 闸门「是否允许审批：否」 | F-003 / §7.2 | ✅ |
| 风险命中 = 不自动出金 | 交易状态「处理中」（暂缓出金）+ 闸门 | §7.3 | ✅ |
| 命中统一进待审 + 人工审核 | 操作列「复审」→ 弹窗闸门（中风险 / 高风险均禁止放行） | F-004 / F-005 / §7 | ✅ |
| 入金拒绝 → 退款 | 拒绝必填理由（空值不可提交）→ 交易状态「已拒绝」+ 后台自动原路退款（退款中 → 已退款） | F-006 / F-014 | ✅（客户端「已退款」文案见 D7；**拒绝理由已回显客户端详情**） |
| 出金拒绝 → 出金失败 | 同上，资金解冻退回可用余额 | F-007 | ✅（同上 D7） |
| 防恶意方试探 | 客户端仅「合规审核」通用文案，不暴露等级 / 命中实体；**唯一例外为经脱敏的拒绝理由**（F-014） | F-010 / F-014 | ✅ |

---

## 三、API 映射正确性审计

| 审计项 | 结果 | 说明 |
|---|---|---|
| 入金筛查 `type=1` / 出金 `type=2` | ✅ 正确 | 弹窗实测 kya-note：出金 type=2、扫描对象为提现地址 |
| 只用 `POST /addresses/`，无 `POST /transactions/` | ✅ 正确 | 原型全文无交易筛查调用；PRD §8 列为验收项 |
| 阈值 `risk_level 0–2` 自动通过 / `3–5` 转人工（D1 已于 2026-09-18 二次修订确认） | ✅ 一致 | 实测：0–2 级 10 笔「已自动通过」按钮禁用；3–5 级 6 笔进入人工复核（放行需复核意见、拒绝需理由） |
| fail-closed（超时 / 未完成不放行） | ✅ 一致 | 扫描失败、待扫描、扫描中 → 闸门「否」 |
| 手动重新扫描 | ✅ 已实现 | 扫描中 → 已完成，回填风险等级 / 结果 / 时间并刷新闸门（臆造的 `x/10` 评分已移除） |
| 风险等级枚举 0–5 与处置映射 | ✅ 一致 | 附录 A.3 六档 |
| 认证 `X-API-KEY` 服务端持有 / 限流 / 背压 | ✅ 已列 | NFR 安全 + 限流 |
| Merkle resolve 回写 | ⚠️ 后台动作 | 原型不呈现（界面仅「写入审核日志」），F-008 保留为后台要求 |
| Webhook 持续监控 | ⚠️ 后续 | F-011（P2，本期不做） |
| 评分展示口径 | ✅ 已解决（D8，2026-09-18） | 原型已删除臆造的 `x / 10` 评分，只用 `risk_level`（0–5）+ `risk_level_verbose` |

---

## 四、原型质量审计

### 4.1 客户端原型（`2026-09-11_加密出入金风险扫描_客户端.html`）
| 项 | 结果 |
|---|---|
| 深色主题（`#111418` / `#FFCA00`）匹配现网 | ✅ |
| 交易状态 `processing` → 文案「该笔交易正在接受合规审核，审核通过后自动完成」 | ✅ |
| 出金结果页新增 `pending`「审核中」/ `declined`「已拒绝」合规文案 | ✅ |
| 不泄露风险等级 / 命中原因 | ✅ |
| `Counterparty Wallet` 列 | ❌ 未落地（D11） |
| 独立「已退款 / 出金失败」徽章 | ❌ 未落地（D7） |

### 4.2 管理端原型（`2026-09-14_加密出入金风险扫描_管理端.html`，41,438 B，本次实测）
| 项 | 结果 |
|---|---|
| 现网浅色外壳 / 主色 `#E59303` / 弹窗 672px · 圆角 10px（`getComputedStyle` 逐条对现网同值） | ✅ |
| 导航：现网 13 节点 + 加密货币管理组内新增「加密风险扫描」 | ✅ |
| 单表 **16 列**（含「类型」列与「退款状态」列）+ 类型筛选 + 分页 10 · 20 · 50 | ✅ |
| 4 张统计卡由数据实时计算（实测：待人工复核 6 / 已自动通过 10 / 筛查未完成 5 / 退款处理中 1） | ✅ |
| 详情弹窗：MS 风险信息块（`risk_level` + 档位 + 风险来源明细）+ 交易信息块 + 汇总块 + **拒绝必填理由** + 自动退款块 + 审批闸门；拒绝后**原因只读展示**（防重复提交） | ✅ 实测 |
| 审批闸门：3–5 级进入人工复核（放行需复核意见、拒绝需理由）；0–2 级「已自动通过」按钮禁用；待扫描 / 扫描中 / 扫描失败禁止放行（fail-closed） | ✅ 实测 |
| 待复核守卫：0–2 级自动通过、已拒绝订单不可再审（拒因框转只读） | ✅ 实测 |
| 手动重新扫描：扫描中 → 已完成，回填结果并刷新闸门 | ✅ |
| 阈值联动：统计卡「待人工复核（3–5 级）」与判定函数 `outcome()` 一致；自动/人工分支 24 行全量核验 | ✅ |
| 浏览器无 JS 异常（console 0 error）/ `node --check` 通过 | ✅ |

---

## 五、交付物清单

| 文件 | 类型 | 路径 |
|---|---|---|
| 需求梳理与注意点 | Markdown | `prd/Merkle-Science-加密出入金风险扫描-需求梳理与注意点.md`（09-14 更新） |
| 客户端原型 | HTML | `prototypes/加密出入金风险扫描/2026-09-11_加密出入金风险扫描_客户端.html` |
| 管理端原型 | HTML | `prototypes/加密出入金风险扫描/2026-09-14_加密出入金风险扫描_管理端.html` |
| PRD | Markdown | `prd/PRD-加密出入金钱包风险扫描-Merkle-Science-v1.0.md`（文档版本 v1.4） |
| 本审计 | Markdown | `prd/加密出入金风险扫描-整体验收审计.md`（v1.3） |

---

## 六、遗留决策（详见 PRD 同名表）

| 状态 | 编号 |
|---|---|
| ✅ 已确认 | D1（风险阈值，2026-09-18 二次修订：`0–2` 自动通过 / `3–5` 转人工）· D8（不使用臆造的 `x/10` 评分，仅 `risk_level`） |
| 待产品 / 合规拍板 | D2 超时兜底 · D3 退款路径 · D6 入金 `type` 语义 · D7 是否新增「已退款 / 出金失败」终态 · D9 手动重新扫描是否纳入本期 · D11 客户端 `Counterparty Wallet` 列 · D12 管理端页名与路由 · **D13 拒绝理由客户端可见范围与脱敏口径 · D14 拒因是否允许二次编辑** |
| 已默认 | D4 持续监控本期不做 · D5 客户级筛查本期不做 · D10 逐条告警明细本期不展示 · **D15 历史单无理由时的客户端兜底文案** |

---

## 七、审计结论

✅ **复审通过（v1.3，2026-09-18 二次修订口径）**：交付物齐全且相互自洽；API 映射正确（仅钱包地址筛查 KYA）；阈值 `0–2 自动通过 / 3–5 转人工`、fail-closed 降级、手动重新扫描、**拒绝理由闭环与客户端回显（F-014）** 均已实测一致；拒绝理由的脱敏红线与可见范围登记为待拍板决策（D13 / D14）。

遗留待确认决策（D2 / D3 / D6 / D7 / D9 / D11–D14）不影响本期交付，已在 PRD 与本文档显式列出。

> 备注：v1.0 结论中的「两条队列」「命中告警明细含置信度」判定已作废，以本版 4.2 节实测结果为准。
