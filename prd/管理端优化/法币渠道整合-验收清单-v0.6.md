# 管理端法币模块 · 验收清单 v0.6（05 币种设置第三轮五项改造）

上一版：v0.5（05 第二轮六项改造，产物 485 行 / 28061 字节）。
本版只覆盖本轮 05 单页改动（其余 5 页判据仍以 v0.4 B 组为准，本轮不改动）。
本轮用户令（原话）：「币种设置移除编辑币种，移除弹窗的生效方式和客户账单展示， 金额挡位需要可以配置。法币的开关也移除。FX 兑换费也都移除。」

1. 移除**「编辑币种」**：卡片头入口按钮（3 处）+ `ccyModal` 整段 + JS `openCcy` / `saveCcy` / `CCY` 常量 + 相关 CSS；
2. 移除**弹窗的「生效方式」与「客户账单展示」**：`feeModal` 两个 `fm-item`（`fmEff` / `fmShow`）；`ccyModal` 内的「生效方式 / 客户可见」随第 1 项一并移除；
3. **「金额档位」改为可配置**：`feeModal` 新增 `fmTierMin` / `fmTierMax` 两个输入；保存回写表格档位单元格 + 按钮载荷 + 重开回填；支持 `≤ X` / `> X` / `X–Y` 区间 / `—` 四种形态；
4. 移除**「法币开关」**：3 处 `.toggle` 控件 + `toggleModal` 整段 + JS `openToggle` / `confirmToggle` / `pendingToggle` + `.toggle` / `.badge-off` CSS（**保留** ENABLED 状态徽标，见 D7）；
5. 移除**「FX 兑换费」**：EUR / GBP 各 1 行，共 2 行（连同其「编辑」入口）。

产物路径：`C:\Users\xieyi\Desktop\YASBe\admin\prototypes\fiat\05-Fiat-Currency-Settings.html`
线上：`https://xieyi0734-art.github.io/yasbe-prototypes/admin/prototypes/fiat/05-Fiat-Currency-Settings.html`
本轮提交：`c428c82`（HEAD == origin/main）
本轮产物规模：**404 行 / 22006 字节**（改前 485 行 / 28061 字节）

## A 结构卫生（对 05 单独机检；A1–A7 继承 v0.4 定义）

- A1 `class="note"` == 0
- A2 字符串「口径」== 0
- A3 字符串「演示」== 0
- A4 `class="sec-sub"` == 0
- A5 文件不含 CR 字符（纯 LF）
- A6 标签配平：`<table` == `</table>`（各 **3**）；`<tr>` == `</tr>`（各 **22**）；`<td` == `</td>`（各 **114**）；`<div` == `</div>`（各 **74**）；`<span` == `</span>`（各 **64**）
- A7 不含 `console.log(`
- A8 `node --check` 抽取后的 JS 通过（rc=0）
- A9 所有 `onclick=` 引用的函数均已定义（现存引用仅 `openFee` / `saveFee` / `closeModal`）
- A10 `<select>` 全文 **0** 处（改前 5：生效方式 / 客户账单展示 / ccyModal 生效方式 / rails 多选等；本轮清空）

## B 组：05 币种设置（本轮五项改造 · 逐条判据）

| # | 断言 | 期望 | 证据 |
|---|---|---|---|
| B68 | 「编辑币种」入口已删 | 字符串「编辑币种」全文 **0** 次（改前 6 次：3 处按钮 + 3 处弹窗注释/标题） | `05` |
| B69 | `ccyModal` 整段已删 | `ccyModal` / `cmTitle` / `cmAcc` / `cmCh` / `cmRails` 均 **0** 次；`document.querySelectorAll('.modal-overlay').length == 1`（仅 `feeModal`） | `05` |
| B70 | 币种编辑 JS 已删 | `function openCcy` / `function saveCcy` / `var CCY` 均 **0** 次；无残留调用 | `05` |
| B71 | 弹窗「生效方式」已删 | 字符串「生效方式」全文 **0** 次；`id="fmEff"` **0** 次 | `05` |
| B72 | 弹窗「客户账单展示」已删 | 字符串「客户账单展示」 **0** 次；`id="fmShow"` **0** 次；「客户可见」**0** 次 | `05` |
| B73 | 费率弹窗字段回归 3 项 | `#feeModal .fm-item label` 文案 == 对外费率（%）/ 固定费用 / 金额档位（可配置）；`#feeModal input` == `fmRate` / `fmFixed` / `fmTierMin` / `fmTierMax` | `05` |
| B74 | 金额档位可配置（下限/上限输入存在） | `id="fmTierMin"` 与 `id="fmTierMax"` 各 **1** 处（+2 处 JS 引用）；标签文案含「金额档位」且标注可配置；输入框非 `readonly` / 非 `disabled` | `05` |
| B75 | 打开弹窗按行回填档位 | 19 行随行回填：`≤ 50,000` → max=`50,000`；`> 50,000` → min=`50,000`；`—` → 两者均空 | 人工/浏览器实测 |
| B76 | 保存写回表格档位 | 改档位为 100,001 / 250,000 → 该行档位单元格文本 == `100,001–250,000`；弹窗关闭 + toast 含新档位 | 人工/浏览器实测 |
| B77 | 保存写回载荷并支持重开 | 保存后重新打开同一行，`fmTierMin` == `100,001`、`fmTierMax` == `250,000`（载荷 `"tier"` 同步替换） | 人工/浏览器实测 |
| B78 | 法币开关控件已删 | `class="toggle` **0** 次；`id="toggleModal"` **0** 次；`openToggle` / `confirmToggle` / `pendingToggle` 均 **0** 次；CSS 无 `.toggle` / `.badge-off` | `05` |
| B79 | 三卡片头仅剩状态徽标 | 三个 `.sec-head` 文本 == `Bridge $ USD · ENABLED` / `OpenPayd € EUR · ENABLED` / `OpenPayd £ GBP · ENABLED`（`ENABLED` 共 **3** 处）；`class="button/btn-outline"` 在卡片头 **0** 处 | 人工/浏览器实测 |
| B80 | FX 兑换费行已删 | 字符串「FX 兑换费」 **0** 次（改前 2 行）；三表行数 == **6 / 7 / 6**（合计 19），改前 21 | `05` |
| B81 | 操作列未被误伤 | `编辑</button>` == **19** 处；`.edit-link` 按钮 == 19；每行 6 个 `<td>`（合计 114） | `05` |
| B82 | 无未闭合 / 畸形标签 | `<td><td` 与 `</button></td></td>` 均 **0** 次 | `05` |
| B83 | 首屏无 JS 报错 | 浏览器 console `js_errors == []` | 人工/浏览器实测 |

## C 组：部署与登记

| # | 断言 | 期望 | 证据 |
|---|---|---|---|
| C1 | 线上 6 页 HTTP 状态 | 200 | `https://xieyi0734-art.github.io/yasbe-prototypes/admin/prototypes/fiat/0N-*.html` |
| C2·05 | 线上字节 == 本地字节 | **22006** | `05-Fiat-Currency-Settings.html` |
| C2·其他 | 01 / 02 / 03 / 04 / 06 本轮未改 | 57513 / 34115 / 368 / 48276 / 23119 | 同上 |
| C3 | git HEAD == origin/main | `c428c82` | 仓库根 `C:\Users\xieyi\Desktop\YASBe` |
| C4 | 本轮范围工作区洁净 | `git status --porcelain -- admin/prototypes/fiat/05-Fiat-Currency-Settings.html PROTOTYPE-LINKS.md` 输出为空 | 同上 |
| C5 | 原型登记已同步 | `PROTOTYPE-LINKS.md` 05 行描述已更新为「费率挂「通道 × 币种 × rails」两档 + 退回费; 1 弹窗(费率编辑 · 金额档位可配置); 无编辑币种 · 无启停开关 · 无 FX 兑换费 · 无通道成本 · 无限额层级」 | 仓库根 `PROTOTYPE-LINKS.md` |

## D 待人工/业务判定（pro 只标注，不改产物）

| # | 事项 | 现状 |
|---|---|---|
| D6 | 生产验证 | 已即时通过 Hermes 浏览器工程验证：开弹窗 / 改档位 / 保存 / 重开回填 / 遮罩关闭 / 0 JS 报错；无其他真机遗留 |
| D7 | **ENABLED 状态徽标是否保留** | 本轮「开关移除」仅删除启停**控件**，三卡片头保留静态 `ENABLED` 状态徽标（已无任何交互可改变其状态）；若业务欲一并删除状态展示，需再改一次 |
| D8 | 编辑费率保存连带的回写 | 为使档位可配置后不出现「档位变了、费率没变」的不一致，`saveFee` 同时把「对外费率 / 固定费用」回写表格与载荷（新增行为，超出「档位可配置」字面要求，待确认是否保留） |
| D9 | 弹窗宽度 | 沿用本页既有 `.modal` 样式（680px）；现网管理端标准 672px / 圆角 10px，本页历史即为 680，未做统一 |
| D10 | 05 侧栏仍列「资金总览」 | 装饰性文本，无链接（同 01/02/06），本轮未动，待拍板 |
| D11 | 02 / 06「导出 CSV」仍用 `alert` | 沿 v0.4 D1，未改 |
| D12 | 表头「金额档位」与弹窗「金额档位（可配置）」文案 | 若业务希望统一为「档位」/「金额区间」等口径，需再改一次 |

## E 报告格式

逐条给 `PASS` / `FAIL` / `UNKNOWN`，每条附 `文件:行号` 或 URL 与原文片段；FAIL 须给修复建议；末尾给总判（达标 / 不达标）与 FAIL 计数。中文输出。
